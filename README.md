# 👞 Фабрика Взуття - Інтернет-магазин

Повнофункціональний інтернет-магазин взуття з системою управління наявністю та конструктором індивідуального взуття.

## 🚀 Функціонал

### Для клієнтів:
- 📱 **Каталог товарів** з фільтрацією по категоріях та сезонах
- 🔍 **Пошук товарів**
- 🖼️ **Галерея фото** - перегляд товару з різних боків
- 📊 **Відображення наявності** на складі по розмірах
- 🛒 **Кошик** та зручне оформлення замовлень
- 🎨 **Конструктор взуття** - створення індивідуального взуття:
  - Вибір моделі
  - Вибір матеріалу (шкіра, нубук, шкірозамінник, хутро, байка)
  - Вибір кольору
  - Вибір підошви

### Для менеджерів:
- 📦 **Управління наявністю** - швидке оновлення кількості товарів на складі
- 📋 **Управління замовленнями** - перегляд та зміна статусів
- 📊 **Перегляд наявності** по всіх товарах та розмірах

## 🛠️ Технології

### Backend:
- Node.js + Express + TypeScript
- MySQL (Sequelize ORM)
- RESTful API

### Frontend:
- React + TypeScript
- Tailwind CSS
- React Router
- React Image Gallery
- React Hot Toast

## 📦 Встановлення та запуск

### Локальна розробка:

#### Backend:

```bash
cd backend
cp .env.example .env  # Налаштуйте змінні оточення
npm install
npm run dev
```

Backend буде доступний на `http://localhost:3001`

#### Frontend:

```bash
cd frontend
npm install
npm run dev
```

Frontend буде доступний на `http://localhost:3000`

### 🚀 Production Deployment:

Для розгортання на production сервері, дивіться детальну інструкцію:

**[📖 DEPLOYMENT.md](./DEPLOYMENT.md)** - Повна інструкція по налаштуванню доменів та deployment

**Коротко:**
1. **Backend**: Налаштуйте `backend/.env` з вашими доменами та БД
2. **Frontend**: Налаштуйте `frontend/.env.production` з URL вашого backend API
3. Детальні інструкції для різних варіантів deployment у файлі DEPLOYMENT.md

## 🗄️ База даних

База даних MySQL автоматично налаштовується при запуску backend.

## 📝 API Endpoints

### Категорії:
- `GET /api/categories` - Отримати всі категорії
- `GET /api/categories/:slug` - Отримати категорію за slug
- `POST /api/categories` - Створити категорію
- `PUT /api/categories/:id` - Оновити категорію
- `DELETE /api/categories/:id` - Видалити категорію

### Товари:
- `GET /api/products` - Отримати всі товари (з фільтрами)
- `GET /api/products/:slug` - Отримати товар за slug
- `POST /api/products` - Створити товар
- `PUT /api/products/:id` - Оновити товар
- `DELETE /api/products/:id` - Видалити товар
- `POST /api/products/:productId/images` - Додати фото товару

### Наявність:
- `GET /api/inventory` - Отримати всю наявність
- `GET /api/inventory/product/:productId` - Наявність для товару
- `GET /api/inventory/check/:productId/:size` - Перевірити наявність
- `PUT /api/inventory/:productId/:size` - Оновити наявність
- `POST /api/inventory/bulk` - Масове оновлення

### Замовлення:
- `POST /api/orders` - Створити замовлення
- `GET /api/orders` - Отримати всі замовлення
- `GET /api/orders/:id` - Отримати замовлення за ID
- `PATCH /api/orders/:id/status` - Оновити статус замовлення

## 🎯 Особливості

### Система наявності:
- Відстеження кількості товарів на складі
- Автоматичне бронювання при створенні замовлення
- Відображення залишків для клієнтів
- Зручне управління для менеджерів

### Конструктор взуття:
- Візуальний вибір параметрів
- Попередній перегляд налаштувань
- Збереження індивідуальних параметрів у замовленні

### Система замовлень:
- Автоматична генерація номеру замовлення
- Перевірка наявності перед створенням
- Відстеження статусу замовлення
- Email та телефон для зв'язку з клієнтом

## 📱 Сторінки

- `/` - Головна сторінка
- `/catalog` - Каталог товарів
- `/product/:slug` - Деталі товару
- `/custom` - Конструктор взуття
- `/cart` - Кошик
- `/checkout` - Оформлення замовлення
- `/manager` - Панель менеджера

## 👥 Автор

Створено для Фабрики Взуття
